class count:
    def __init__(self,add,myfilename,name):
        self.add = add
        self.myfilename = myfilename
        def Read(self):
            with open (self.myfilename, "r") as file:
                lines = file.readline()
                for line in lines:
                    print(lines)
                    def write(self):
                        with open (self.myfilename, "w") as file:
                            file.write(self.add)
                            def find_character(self, char):
                                return self.name.find(char)
                            def find_lenght(self, len):
                                name = len
                               print(name)